name: Go Tests and Examples

# This workflow runs tests and verifies examples for the Composer Crawler project
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    name: Run Tests and Examples
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.20'

      - name: Get dependencies
        run: go get -v -t -d ./...

      - name: Run Tests
        run: go test -v ./pkg/...

      - name: Generate Coverage Report
        run: |
          mkdir -p coverage
          go test -coverprofile=coverage/coverage.out ./pkg/...
          go tool cover -func=coverage/coverage.out
        
      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          
      - name: Verify Examples Compile
        run: |
          echo "Compiling examples to verify they are correct..."
          go build -o /dev/null ./examples/basic_setup/main.go
          go build -o /dev/null ./examples/download_index/main.go
          go build -o /dev/null ./examples/list_packages/main.go
          go build -o /dev/null ./examples/get_statistics/main.go
          go build -o /dev/null ./examples/security_advisories/main.go
          echo "All examples compiled successfully!"
